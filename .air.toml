# .air.toml
# 👇 base folder (your project root)
root = "."
tmp_dir = "tmp"

[build]
# 🧠 This is the command Air runs every time you change code
cmd = "go run cmd/server/main.go"

# We don't actually build a binary; just run directly


# 👀 Watch for these file types
include_ext = ["go", "tpl", "tmpl", "html"]

# 🚫 Ignore these directories to avoid unnecessary rebuilds
exclude_dir = ["tmp", "vendor", "node_modules"]

# Optional log style
log = "air.log"
